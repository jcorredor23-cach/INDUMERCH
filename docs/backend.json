{
  "entities": {
    "RawMaterial": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "RawMaterial",
      "type": "object",
      "description": "Represents a raw material used in the steel foundry, including its stock level and minimum stock threshold.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the raw material."
        },
        "name": {
          "type": "string",
          "description": "Name of the raw material (e.g., ChatarraAcero, ArenaMoldeo)."
        },
        "stock": {
          "type": "number",
          "description": "Current stock level of the raw material."
        },
        "unit": {
          "type": "string",
          "description": "Unit of measurement for the raw material (e.g., ton, kg, m3)."
        },
        "min_stock": {
          "type": "number",
          "description": "Minimum stock level that should be maintained for the raw material."
        }
      },
      "required": [
        "id",
        "name",
        "stock",
        "unit",
        "min_stock"
      ]
    },
    "Client": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Client",
      "type": "object",
      "description": "Represents a client of the steel foundry.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the client."
        },
        "name": {
          "type": "string",
          "description": "Name of the client."
        },
        "contact": {
          "type": "string",
          "description": "Contact person for the client."
        }
      },
      "required": [
        "id",
        "name",
        "contact"
      ]
    },
    "ProductionOrder": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ProductionOrder",
      "type": "object",
      "description": "Represents a production order (colada) in the steel foundry.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the production order."
        },
        "op_id": {
          "type": "string",
          "description": "Production order identifier (e.g., COLADA-001)."
        },
        "status": {
          "type": "string",
          "description": "Current status of the production order (e.g., Pendiente, En Proceso, Terminada)."
        },
        "product": {
          "type": "string",
          "description": "Product being manufactured in the production order."
        },
        "qty": {
          "type": "number",
          "description": "Quantity of the product to be manufactured."
        },
        "materials": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "materialId": {
                "type": "string"
              },
              "consumption": {
                "type": "number"
              }
            }
          }
        },
        "targetWeek": {
          "type": "number",
          "description": "Target week for the production order (ISO week number)."
        },
        "client_id": {
          "type": "string",
          "description": "Reference to Client. (Relationship: Client 1:N ProductionOrder). Identifier of the client associated with the production order."
        },
        "priority": {
          "type": "string",
          "description": "Priority of the production order (e.g., Baja, Media, Alta)."
        },
        "job_type": {
          "type": "string",
          "description": "Type of job for the production order (e.g., Normal Production, Express/Small Job)."
        },
        "start_time_est": {
          "type": "number",
          "description": "Estimated start time of the production order (timestamp)."
        },
        "end_time_est": {
          "type": "number",
          "description": "Estimated end time of the production order (timestamp)."
        },
        "createdAt": {
          "type": "number",
          "description": "Timestamp when the production order was created."
        },
        "start_time_real": {
          "type": "number",
          "description": "Actual start time of the production order (timestamp)."
        },
        "end_time_real": {
          "type": "number",
          "description": "Actual end time of the production order (timestamp)."
        },
        "startWeek": {
          "type": "number",
          "description": "Week the OP was started."
        },
        "completionWeek": {
          "type": "number",
          "description": "Week the OP was completed."
        }
      },
      "required": [
        "id",
        "op_id",
        "status",
        "product",
        "qty",
        "materials",
        "targetWeek",
        "client_id",
        "priority",
        "job_type",
        "start_time_est",
        "end_time_est",
        "createdAt"
      ]
    },
    "Incident": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Incident",
      "type": "object",
      "description": "Represents an incident or exception that occurred during production.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the incident."
        },
        "type": {
          "type": "string",
          "description": "Type of incident (e.g., Falla Equipo, Error Humano)."
        },
        "description": {
          "type": "string",
          "description": "Description of the incident."
        },
        "op_id": {
          "type": "string",
          "description": "Reference to ProductionOrder. (Relationship: ProductionOrder 1:N Incident). Identifier of the production order associated with the incident (optional)."
        },
        "timestamp": {
          "type": "number",
          "description": "Timestamp when the incident occurred."
        }
      },
      "required": [
        "id",
        "type",
        "description",
        "timestamp"
      ]
    },
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user's profile.",
      "properties": {
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "createdAt": {
          "type": "number",
          "description": "Timestamp when the user profile was created."
        }
      },
      "required": [
        "email",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/entities/UserProfile"
          },
          "description": "Stores user profile information."
        }
      },
      {
        "path": "users/{userId}/materia_prima_fundicion/{rawMaterialId}",
        "definition": {
          "entityName": "RawMaterial",
          "schema": {
            "$ref": "#/entities/RawMaterial"
          },
          "description": "Stores raw material data for the steel foundry, owned by a user."
        }
      },
      {
        "path": "users/{userId}/clients/{clientId}",
        "definition": {
          "entityName": "Client",
          "schema": {
            "$ref": "#/entities/Client"
          },
          "description": "Stores client data for the steel foundry, owned by a user."
        }
      },
      {
        "path": "users/{userId}/ordenes_produccion_fundicion/{productionOrderId}",
        "definition": {
          "entityName": "ProductionOrder",
          "schema": {
            "$ref": "#/entities/ProductionOrder"
          },
          "description": "Stores production order data for the steel foundry, owned by a user."
        }
      },
      {
        "path": "users/{userId}/incidents/{incidentId}",
        "definition": {
          "entityName": "Incident",
          "schema": {
            "$ref": "#/entities/Incident"
          },
          "description": "Stores incident data for the steel foundry, owned by a user."
        }
      }
    ]
  }
}
